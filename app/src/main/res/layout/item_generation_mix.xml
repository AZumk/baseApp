<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="@dimen/standard_margin_double"
    android:paddingEnd="@dimen/standard_margin_double"
    android:paddingBottom="@dimen/standard_margin">

    <TextView
        android:id="@+id/fuelType"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/fuelPercent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginEnd="@dimen/standard_margin"
        android:gravity="end"
        android:text="Solar"
    />

    <ProgressBar
        android:id="@+id/fuelPercentBar"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/fuelType"
        android:layout_marginStart="@dimen/standard_margin"
        android:progressTint="@color/lightMoss"
        android:indeterminate="false"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:scaleY="4"
        android:max="100"
        android:progress="25"
        android:alpha="0.25"
        />

    <TextView
        android:id="@+id/fuelPercent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/fuelType"
        android:layout_marginStart="@dimen/standard_margin"
        android:text="99%"
        />
</androidx.constraintlayout.widget.ConstraintLayout>